/**
 * Validation library for self-validating domain objects.
 *
 * <p>Provides a fluent rule-based validation framework with monadic error handling
 * via the {@code Outcome} sealed interface.</p>
 */
package be.jsilkens.cortex.common.domain.validation;
